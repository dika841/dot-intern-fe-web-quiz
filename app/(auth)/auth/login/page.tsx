import { LoginModule } from "@/components/pages";
import { NextPage } from "next";
import { ReactElement } from "react";

const Login: NextPage = (): ReactElement => {
  return <LoginModule />;
};
export default Login;
